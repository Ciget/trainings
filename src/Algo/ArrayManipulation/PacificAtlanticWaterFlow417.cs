using System.Collections.Generic;

namespace Algo.ArrayManipulation
{
    public class PacificAtlanticWaterFlow417
    {
        public List<List<int>> PacificAtlantic(int[][] heights)
        {
            List<List<int>> _result = new List<List<int>>();
            List<int> d = new List<int>(1);


            return _result;
            //int minColIndex=0, maxColIndex = 0;
            //int maxHeight = 0;
            //List<List<int>> _result = new List<List<int>>();

            //for (int rowIndex=0; rowIndex< heights.Length; rowIndex++)
            //{
            //    minColIndex = maxColIndex = maxHeight = 0;
            //    for (int columnIndex = 0; columnIndex < heights[rowIndex].Length; columnIndex++)
            //    {
            //        if (heights[rowIndex][columnIndex] > maxHeight)
            //        {
            //            minColIndex = columnIndex;
            //            maxColIndex = columnIndex;
            //            maxHeight = heights[rowIndex][columnIndex];
            //        }
            //        else if (heights[rowIndex][columnIndex] == maxHeight)
            //        {
            //            maxColIndex = columnIndex;
            //        }
            //        else
            //        {
            //        }
            //    }

            //    for (int ti = 0; ti <= maxColIndex - minColIndex; ti++)
            //    {
            //        _result.Add(new List<int>() { rowIndex, ti });
            //    }
            //}

            //return _result;
        }

    }
}

